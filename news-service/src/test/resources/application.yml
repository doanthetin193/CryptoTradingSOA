spring:
  application:
    name: news-service
  cloud:
    discovery:
      enabled: false
    service-registry:
      auto-registration:
        enabled: false
    consul:
      enabled: false
      discovery:
        enabled: false
      config:
        enabled: false

logging:
  level:
    com.cryptotrading.news: WARN
